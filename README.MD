Install Python if not installed.
Navigate to the backend folder (you'll need to create it with the files below).
Run pip install -r requirements.txt.
Run uvicorn main:app --reload to start the server.